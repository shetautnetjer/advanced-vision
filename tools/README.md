# Helper Scripts

This directory contains repo-level helper scripts for capture, training, and
one-off local workflows.

These are not the same thing as the Python package tools under
`src/advanced_vision/tools/`, which power the active MCP computer-use surface.

Use this directory for:

- screenshot and dataset capture helpers
- local training preparation scripts
- disposable workflow utilities
